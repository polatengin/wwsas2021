name: ci_api_user

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "src/api-user/**"

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: azure cli login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
