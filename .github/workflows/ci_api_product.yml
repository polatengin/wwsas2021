name: ci_api_product

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "src/api-product/**"

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: azure cli login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
